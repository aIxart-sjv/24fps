# VITE_API_BASE_URL: base URL of the real 24FPS FastAPI backend's
# versioned API (see backend/app/main.py's API_V1_PREFIX). Defaults to
# http://localhost:8000/api/v1 (uvicorn's default port) when unset.
VITE_API_BASE_URL="http://localhost:8000/api/v1"
