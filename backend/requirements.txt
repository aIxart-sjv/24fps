# GENERATED COMPATIBILITY FILE — DO NOT HAND-EDIT.
# Canonical dependency source is pyproject.toml. Regenerate this file
# with: pip freeze --exclude-editable > requirements.txt
# (run inside an activated venv that has only this project installed).
#
# Phase 13: installing the new AI dependencies caused `uv pip install` to
# re-resolve the whole environment against pyproject.toml's existing
# version ranges, transitively refreshing several already-present packages
# (fastapi, uvicorn, pydantic, pydantic-settings, sqlalchemy, alembic,
# pytest, httpx, black) to newer in-range versions. All Phase 1-12 tests
# were re-verified passing under these versions before this file was
# updated to match.
#
# Phase 18: added reportlab (PDF report rendering) only. This install did
# not re-resolve or change the version of any other package (pillow was
# already present as an opencv/ultralytics dependency).
#
# Phase 21: added bcrypt (password hashing for the new minimal
# authentication boundary) and qrcode (custody handoff QR generation,
# reusing the already-present pillow for image rendering). Neither
# install re-resolved or changed the version of any other package.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
sqlalchemy==2.0.52
alembic==1.19.1
libewf-python==20240506
ultralytics==8.4.135
opencv-python==5.0.0.93
lap==0.5.13
reportlab==4.5.1
bcrypt==4.3.0
qrcode==8.2

# --- dev/test dependencies (install with: pip install -e ".[dev]") ---
pytest==8.4.2
pytest-cov==5.0.0
httpx==0.28.1
black==24.10.0
ruff==0.5.5
mypy==1.10.1
pypdf==5.9.0
